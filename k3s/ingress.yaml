apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: app-service
  namespace: app
spec:
  entryPoints:
    - web
  routes:
  - match: PathPrefix(`/`)
    kind: Rule
    services:
    - name: service-app
      port: 80
